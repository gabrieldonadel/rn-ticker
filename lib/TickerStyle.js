import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  measure: {
    opacity: 0,
  },
  row: {
    overflow: 'hidden',
    flexDirection: 'row',
    justifyContent: 'center',
  },
})
